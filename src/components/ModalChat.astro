---
const currentUrl: URL = new URL(window.location.href);

console.log(currentUrl.href); // This will print the entire URL including protocol, hostname, and path
console.log(currentUrl.origin); // This will print just the protocol and hostname (e.g., https://www.example.com)
---

<h1>{currentUrl}</h1>